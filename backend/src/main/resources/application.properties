spring.application.name=StudySquad API

# PostgreSQL Database Configuration
spring.datasource.url=jdbc:postgresql://localhost:5432/studysquad
spring.datasource.username=postgres
spring.datasource.password=Krishna@4907
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA / Hibernate Properties
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# Web Properties
server.port=8080

# CORS Configuration (To allow React on port 5173 to connect)
# Handled via configuration class
